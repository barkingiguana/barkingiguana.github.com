---
title: "You don't need to return explicitly!"
layout: post
author: Craig
---
<p>Ruby returns the value of the last line executed.</p>

<p>Don't do this:</p>

<pre><code class="ruby">def foo
  value = Foo.first(:conditions => { :label => "bar" })
  return value
end</code></pre>

<p>Do this instead:</p>

<pre><code class="ruby">def foo
  Foo.first(:conditions => { :label => "bar" })
end</code></pre>
